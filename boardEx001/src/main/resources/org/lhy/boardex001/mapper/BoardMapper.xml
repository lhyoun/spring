<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.lhy.boardex001.mapper.BoardMapper">

	<sql id="criteria">
		<trim prefix="(" suffix=") AND " prefixOverrides="OR">
			<foreach collection="typeArr" item="type">
				<trim prefix="OR">
					<choose>
						<when test="type=='T'.toString()">
							title like '%'||#{keyword}||'%'
						</when>
						<when test="type=='C'.toString()">
							content like '%'||#{keyword}||'%'
						</when>
						<when test="type=='W'.toString()">
							writer like '%'||#{keyword}||'%'
						</when>
					</choose>
				</trim>
			</foreach>
		</trim>
	</sql>


	<select id="getList"
		resultType="org.lhy.boardex001.domain.BoardVo">
		select * from board where bno>0
	</select>

	<select id="getListWithPaging" resultType="org.lhy.boardex001.domain.BoardVo">
	  	<![CDATA[
	  		select bno,title,content,writer,regdate,updatedate
	  		from (
	  			select /*+INDEX_DESC(board pk_board)*/
	  			rownum rn, bno, title, content, writer, regdate, updatedate
	  			from board where 
	  	]]>

		<include refid="criteria"></include>
  	
	  	<![CDATA[		
	  			rownum <=#{pageNum} * #{amount}
	  			)
	  			where rn>(#{pageNum}-1)* #{amount}
	  	]]>
	</select>

	<select id="totalCount" resultType="int">
  		<![CDATA[
  			select count(*) from board where 
		]]>

		<include refid="criteria"></include>
   
	  <![CDATA[	
	  	bno>0
	  ]]>

	</select>

	<select id="read" resultType="org.lhy.boardex001.domain.BoardVo" parameterType="long">
		select * from board where bno = #{bno}
	</select>

	<insert id="insert" parameterType="org.lhy.boardex001.domain.BoardVo">
		insert into board(bno, title, content, writer) 
		values(seq_board.nextval, #{title}, #{content}, #{writer})
	</insert>

	<insert id="insertSelectKey">
		<selectKey keyProperty="bno" order="BEFORE" resultType="long">
			select seq_board.nextval from dual
		</selectKey>
		insert into board(bno, title, content, writer) values(#{bno}, #{title}, #{content}, #{writer})
	</insert>

	<delete id="delete">
		delete from board where bno=#{bno}
	</delete>

	<update id="update">
		update board set title=#{title}, content=#{content}, writer=#{writer}, updatedate=sysdate where bno=#{bno}
	</update>

	<!-- <select id="getListWithPaging" resultType="org.lhy.boardex001.domain.BoardVo"> 
		<![CDATA[ select rn, bno, title, content, writer, regdate, updatedate from( 
		select /*+INDEX_DESC(board pk_board)*/ rownum rn, bno, title, content, writer, 
		regdate, updatedate from board where rownum <=#{pageNum} * #{amount} ) where 
		rn>(#{pageNum}-1)*#{amount} ]]> </select> -->

	<!-- <select id="totalCount" resultType="int"> <![CDATA[ select count(*) 
		from board where bno>0 ]]> bno>0 하는 이유는 index를 사용해서 빠르게 가져오라고 </select> -->
</mapper>